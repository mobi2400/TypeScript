let greetins: string = "hello Mobi" // now greeting cannot be assign any other data type


console.log(greetins);

// numbers
let userId: number =333456

//boolean
let isLogedIn:boolean =false

let hero:string;

function getHero(){
    return "thor"
}


hero= getHero() // before assigning the type it shows any(not good)
// in any it doesnt check var type. usually use to escape or skip type-check

export {} // remove teh errors temporarly